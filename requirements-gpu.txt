# requirements-gpu.txt — College GPU machines (CUDA)
#
# BEFORE INSTALLING — check your CUDA driver version:
#   nvidia-smi
#
# Driver >= 525  →  use cu121  (this file)
# Driver  < 525  →  change cu121 to cu118 in the lines below
#
# Install:
#   pip install -r requirements-gpu.txt --break-system-packages

flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.0+cu121
torchvision==0.18.0+cu121

# Fonts required for Telegraph synthetic dataset (Ubuntu/Debian):
# sudo apt-get install -y fonts-liberation fonts-freefont-ttf
